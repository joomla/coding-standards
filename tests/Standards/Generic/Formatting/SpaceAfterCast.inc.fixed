<?php

// Valid: A cast operator is followed by one space.
$foo = (string) 1;

// Invalid: A cast operator is not followed by whitespace.
$foo = (string) 1;

// Invalid: A cast operator is followed by more than one space.
$foo = (string) 1;
